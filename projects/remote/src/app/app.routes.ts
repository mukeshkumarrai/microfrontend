import { Routes } from '@angular/router';
import { AppComponent } from './app.component'; // Import the main component

export const routes: Routes = [
  {
    path: '',
    component: AppComponent, // Set the main component for the default route
  },
];
